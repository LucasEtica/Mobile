import React, { useState } from 'react';
import { View, TextInput, Button } from 'react-native';
import InputTextLogin from '../componentes/InputTextLogin';

const Login = (props) => {
    return <View style={{backgroundColor: "#D6DFCC",}}>
        <InputTextLogin />
    </View>
};

export default Login;